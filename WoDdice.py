import random
##outcome can be a dramatic failure, failure, n successes, exceptional success
def roll(dicepool):
    outcome = range(dicepool)
    for n in outcome:
        outcome[n] = random.randint(1,10)
    successes = 0
    for n in outcome:
        if
    
    if dicepool == 0: ##chance die. success only on 10

##def instantAction(dicepool)
##def extendedAction(dicepool)
##def roteAction(dicepool)
        
    
